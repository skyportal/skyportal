import { FC } from "react";

// AboutPlugins.tsx is generated from AboutPlugins.tsx.template at build time,
// so it is absent when tsc runs on a fresh checkout.
declare const AboutPlugins: FC;
export default AboutPlugins;
